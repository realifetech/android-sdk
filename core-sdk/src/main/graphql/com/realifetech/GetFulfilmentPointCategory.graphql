query getFulfilmentPointCategories($pageSize: Int!, $page: Int = 1) {
  me {
    fulfilmentPointCategories(pageSize: $pageSize, page: $page) {
      edges { ...FragmentFulfilmentPointCategory }
      nextPage
      lastPage
      firstPage
    }
  }
}