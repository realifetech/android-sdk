fragment FragmentWidget on WidgetEdge {
    edges {
        id
        style {
            type
            size
            padded
        }
        viewAllUrl
        widgetType
        variation {
            fetchType
            contentIds
            params {
                key
                value
            }
            engagementParams {
                key
                value
            }
            translations {
                language
                title

            }
        }
    }
    nextPage
}