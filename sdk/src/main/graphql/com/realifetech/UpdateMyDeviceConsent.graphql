mutation UpdateMyDeviceConsent($input: DeviceConsentInput!) {
    updateMyDeviceConsent(input: $input) {
        id
        calendar
        camera
        locationCapture
        locationGranular
        photoSharing
        pushNotification
    }
}