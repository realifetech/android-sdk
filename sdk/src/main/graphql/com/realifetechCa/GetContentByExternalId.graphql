query getContentByExternalId($externalId: String!) {
    getContentByExternalId(externalId: $externalId) {
        campaignId,
        items {
            contentType,
            data
        }
    }
}